# Loaded by `vite --mode demo` (npm run demo). Locks the sender to the bundled
# payloads: no file picker, no text snippets. Kept as a mode file rather than an
# inline env var so the scripts work identically on Windows.
VITE_DEMO=1
